{
  "description": "Plugin hooks: block WebFetch against client-rendered hosts (YouTube, Reddit) and steer to a tool that works; and on stop, challenge a confident unsourced assertion so the assistant substantiates it before finishing.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/block-spa-webfetch.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/challenge-detector.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
