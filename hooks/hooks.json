{
  "description": "Plugin hook: block WebFetch against client-rendered hosts (YouTube, Reddit) and steer to a tool that works. The challenge-detector Stop hook is offline (see hooks/challenge-detector.py): a blocking Stop hook fires inside automated `claude -p` worker sessions and breaks them. Re-enable once it can gate to interactive sessions only.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "WebFetch",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/block-spa-webfetch.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
